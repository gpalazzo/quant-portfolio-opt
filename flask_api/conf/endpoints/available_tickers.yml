# Swagger API docs for `available_tickers` endpoint

summary: "Get available tickers for optimization"
description: "Return the list of available tickers for optimization"

produces:
- "application/json"

responses:
  200:
    description: Tickers retrieved successfully